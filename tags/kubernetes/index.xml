<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>kubernetes on One Dave's Blog</title><link>https://djosephsen.github.io//tags/kubernetes/</link><description>Recent content in kubernetes on One Dave's Blog</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Sun, 13 Sep 2026 14:32:20 -0500</lastBuildDate><atom:link href="https://djosephsen.github.io//tags/kubernetes/index.xml" rel="self" type="application/rss+xml"/><item><title>Kubernetes for Haters</title><link>https://djosephsen.github.io//posts/k8s1/</link><pubDate>Sun, 13 Sep 2026 14:32:20 -0500</pubDate><guid>https://djosephsen.github.io//posts/k8s1/</guid><description>&lt;p>Kubernetes is the single most poorly understood piece of software ever created. This should perhaps be surprising given how wildly successful it is, but the truth is even more surprising: Kubernetes is misunderstood &lt;em>by design&lt;/em>. I can&amp;rsquo;t account for why this would be, but the fact of it is unquestionable. The entire body of documentation along with the video-based and interpersonally delivered tutoring provided by the experts is, as far as I can tell, metaphorical. And by metaphorical, I mean lies. The material and people are persistently lying about what Kubernetes is and how it works. I&amp;rsquo;m not attributing to malice. I think it&amp;rsquo;s just more of a vibe they&amp;rsquo;re trying to strike that goes something like: &lt;em>You don&amp;rsquo;t need to know how it works to use it, so here&amp;rsquo;s what you need to know&lt;/em>. That&amp;rsquo;s inference. I can&amp;rsquo;t speak authoritatively to intent, but the end-result is objectively a user-space who is utterly baffled by Kubernetes and completely ignorant of how it works in real life.&lt;/p>
&lt;p>You might think I&amp;rsquo;m being contentious; perhaps I am. Candidly I&amp;rsquo;m a little salty about it. My observation is that professional engineers in general make their way through the world by learning how things work. They don&amp;rsquo;t enjoy being told they &lt;em>don&amp;rsquo;t need to know&lt;/em>, nor do they have much use for poorly formed metaphors about what something is &lt;em>kinda like&lt;/em>. Suffice to say, I&amp;rsquo;m not a fan of the current approach taken by the documentation available &amp;ndash; official and community. If you read through this article I believe you will not only learn something you didn&amp;rsquo;t know about Kubernetes, but also come to agree with me, or at least concede I have a point. If there&amp;rsquo;s a text that attempts to teach people about Kubernetes in its real life, non-metaphorical form, then I apologise, I&amp;rsquo;m yet to find it, and what exists in its stead strikes me as counterproductive, especially given how simple the truth of its architecture is to learn from first principles, and how much more difficult it is to disambiguate things once you&amp;rsquo;ve ingested the officially blessed metaphor.&lt;/p>
&lt;p>Give me 30 minutes, and I&amp;rsquo;ll show you &lt;strong>everything&lt;/strong> going on inside this &amp;ldquo;Kubernetes&amp;rdquo; thing.&lt;/p>
&lt;h2 id="the-yealots">The Yealots&lt;/h2>
&lt;p>Before we begin, I need to make a small digression into terminology. Specifically, I need a pejorative term for &lt;em>them&lt;/em>. You know who I mean. The &lt;em>cloud native&lt;/em>. The ones who, for whatever reason, don&amp;rsquo;t want us to know how Kubernetes actually works. I would call them &lt;em>natives&lt;/em> but for the troubling colonizer overtones.&lt;/p>
&lt;p>Let&amp;rsquo;s see. Dogmatic, religiously devoted to their one solution, and filled to the brim with theatrical metaphor that doesn&amp;rsquo;t really even orbit the truth. They remind me more of old-school communist gaslighter types. Posadists or Lysenkoists, shouting the party line at the top of their lungs and expecting reality to align to the ideology but not really caring whether it ever does. Yaml Zealots. Yealots? Hah. Yealots. We&amp;rsquo;ll go with that.&lt;/p>
&lt;h2 id="there-is-no-kubernetes">There is no &amp;ldquo;Kubernetes&amp;rdquo;&lt;/h2>
&lt;p>The Yealots want us to believe Kubernetes is a bulbous sort of centralized orchestration monolith. &lt;strong>Kubernetes&lt;/strong> is the thing that scales stuff up and down, &lt;strong>Kubernetes&lt;/strong> deploys things, &lt;strong>Kubernetes&lt;/strong> handles RBAC etc&amp;hellip; Perhaps they sometimes accidentally let slip the mention of a &amp;ldquo;controller&amp;rdquo; or two &amp;ndash; the pod controller, the deployment controller, but &lt;strong>Kubernetes&lt;/strong> runs them. Yeah that&amp;rsquo;s what it does, &lt;em>Kubernetes runs controllers&lt;/em>. Right?&lt;/p>
&lt;p>Perhaps the Yealots believe these things themselves? I honestly can&amp;rsquo;t tell. For example, the first thing you&amp;rsquo;ll learn from the official docs is that the word &lt;em>Kubernetes&lt;/em> itself means something like &amp;ldquo;helmsman&amp;rdquo; in ancient Greek. So they&amp;rsquo;ve named this solution after a single person steering a ship. So how could Kubernetes &lt;strong>not be&lt;/strong> a monolithic orchestrator given the very name is a &lt;strong>huge commitment&lt;/strong> to a metaphor for a monolithic orchestrator? So there&amp;rsquo;s gotta be a &lt;strong>central thing&lt;/strong>, called Kubernetes which is steering this ship. Right?&lt;/p>
&lt;p>&lt;strong>Lies&lt;/strong> my friend. There is no monolithic entity in the system that you can point at which is called Kubernetes. There is no Kubernetes process, nor is there a Kubernetes binary. There is no controller-controller &amp;ndash; no captain is steering this ship. In point of fact, &lt;em>Kubernetes&lt;/em> is just a proper noun we&amp;rsquo;ve assigned to a strangely shaped microservices architecture that takes just about the opposite shape that its name implies. A distributed system where no service is called Kubernetes, and every service is decoupled from every other by means of a single, centralized API endpoint, which is &lt;em>also not called Kubernetes&lt;/em>.&lt;/p>
&lt;h2 id="meet-kube-apiserver">Meet kube-apiserver&lt;/h2>
&lt;p>In point of fact, this central API server is called &amp;ldquo;kube-apiserver&amp;rdquo;, and there&amp;rsquo;s absolutely nothing magic about it. I say that, because given the involvement of Golang and containers and cloud-nativity in general, and given the silence from the docs about how messages are actually sent, and the communities apparent aversion to talking about how this works, you might assume some complex binary protocols are involved. Webassembly, GRPC, Protobuf, fancy magic-pants stuff it&amp;rsquo;s difficult to wrap one&amp;rsquo;s head around.&lt;/p>
&lt;p>Nope. Kube-apiserver speaks HTTP. It&amp;rsquo;s just a plain-ole restful HTTP API server. And because it&amp;rsquo;s literally the only interface in the system, that means &lt;em>every service&lt;/em> in Kubernetes speaks plain ole HTTP.&lt;/p>
&lt;p>Ok that&amp;rsquo;s fine for &lt;em>this interface&lt;/em>, but what about all the other Kubernetes interfaces?&lt;/p>
&lt;p>&lt;em>There aren&amp;rsquo;t any other interfaces.&lt;/em> There is only kube-apiserver. This singular interface is used by Humans to do things like get pod status, by machines to do things like deploy new workloads, and even by the internal Kubernetes services to communicate with one another, to, for example, change the state of a pod from &lt;code>Ready&lt;/code> to &lt;code>CrashLoopBackoff&lt;/code>. &lt;strong>All operations, human-beings, RPC, and automation, use the same interface: kube-apiserver&lt;/strong>&lt;/p>
&lt;p>&lt;em>Well that&amp;rsquo;s obvious bullcrap Dave&lt;/em> I can hear you say. &lt;em>What about Apply, Patch, and Update? There are more operations than HTTP GET and POST.&lt;/em>&lt;/p>
&lt;p>The operations the Yealots talk about like those performed by commands like &lt;code>kubectl apply&lt;/code> are details encapsulated in the body of an HTTP request and sent to kube-apiserver by kubectl. Lets take a look at one.&lt;/p>
&lt;pre>&lt;code>POST /apis/apps/v1/namespaces/foo/deployments?fieldManager=kubectl-client-side-apply HTTP/1.1
Host: 10.96.0.1:443
Authorization: Bearer eyJhbGciOiJSUzI1NiIsImtpZCI6IjRPYWQ3NjZ...
Content-Type: application/json
Accept: application/json
{
&amp;quot;apiVersion&amp;quot;: &amp;quot;apps/v1&amp;quot;,
&amp;quot;kind&amp;quot;: &amp;quot;Deployment&amp;quot;,
&amp;quot;metadata&amp;quot;: {
&amp;quot;name&amp;quot;: &amp;quot;foo&amp;quot;,
&amp;quot;namespace&amp;quot;: &amp;quot;foo&amp;quot;,
&amp;quot;annotations&amp;quot;: {
&amp;quot;kubectl.Kubernetes.io/last-applied-configuration&amp;quot;: &amp;quot;{\&amp;quot;apiVersion\&amp;quot;:\&amp;quot;apps/v1\&amp;quot;,\&amp;quot;kind\&amp;quot;:\&amp;quot;Deployment\&amp;quot;,\&amp;quot;metadata\&amp;quot;:{\&amp;quot;name\&amp;quot;:\&amp;quot;foo\&amp;quot;,\&amp;quot;namespace\&amp;quot;:\&amp;quot;foo\&amp;quot;},\&amp;quot;spec\&amp;quot;:{\&amp;quot;replicas\&amp;quot;:1,\&amp;quot;selector\&amp;quot;:{\&amp;quot;matchLabels\&amp;quot;:{\&amp;quot;app\&amp;quot;:\&amp;quot;foo\&amp;quot;}},\&amp;quot;template\&amp;quot;:{\&amp;quot;metadata\&amp;quot;:{\&amp;quot;labels\&amp;quot;:{\&amp;quot;app\&amp;quot;:\&amp;quot;foo\&amp;quot;}},\&amp;quot;spec\&amp;quot;:{\&amp;quot;containers\&amp;quot;:[{\&amp;quot;name\&amp;quot;:\&amp;quot;foo\&amp;quot;,\&amp;quot;image\&amp;quot;:\&amp;quot;nginx:1.27\&amp;quot;}]}}}}\n&amp;quot;
}
},
&amp;quot;spec&amp;quot;: {
&amp;quot;replicas&amp;quot;: 1,
&amp;quot;selector&amp;quot;: {
&amp;quot;matchLabels&amp;quot;: { &amp;quot;app&amp;quot;: &amp;quot;foo&amp;quot; }
},
&amp;quot;template&amp;quot;: {
&amp;quot;metadata&amp;quot;: { &amp;quot;labels&amp;quot;: { &amp;quot;app&amp;quot;: &amp;quot;foo&amp;quot; } },
&amp;quot;spec&amp;quot;: {
&amp;quot;containers&amp;quot;: [
{ &amp;quot;name&amp;quot;: &amp;quot;foo&amp;quot;, &amp;quot;image&amp;quot;: &amp;quot;nginx:1.27&amp;quot; }
]
}
}
}
}
&lt;/code>&lt;/pre>&lt;p>This is what kubectl sends kube-apiserver when you &lt;code>apply&lt;/code> a &lt;code>deployment&lt;/code>. It&amp;rsquo;s an HTTP request with a &lt;a href="https://pkg.go.dev/k8s.io/api/apps/v1#Deployment">serialized data-structure of type &lt;code>Deployment&lt;/code>&lt;/a> in the body. This is the shape every kubectl command you run against a cluster takes. Kubectl creates an HTTP request like this one from your personal computer to the kube-apiserver endpoint described by your current Kubernetes context using a bearer token extracted from your local context. That&amp;rsquo;s all that&amp;rsquo;s happening. You could do the same thing with cURL.&lt;/p>
&lt;p>Lets look at what happens when we &lt;code>kubectl get pods&lt;/code>&lt;/p>
&lt;pre>&lt;code>GET /api/v1/namespaces/foo/pods HTTP/1.1
Host: 10.96.0.1:443
Authorization: Bearer eyJhbGciOiJSUzI1NiIsImtpZCI6IjRPYWQ3NjZ...
Accept: application/json;as=Table;v=v1;g=meta.k8s.io,application/json
&lt;/code>&lt;/pre>&lt;p>When you call &lt;code>kubectl get pods&lt;/code> for example, a GET request like the one above is created by kubectl and sent to the kube-apiserver endpoint. Kube-apiserver responds with a list of pods, which kubectl reformats and parrots back out to you.&lt;/p>
&lt;p>&lt;em>Well that&amp;rsquo;s obvious bullcrap Dave&lt;/em> I can hear you say. &lt;em>I can kubectl get logs and return a stream of log messages in my terminal. And everyone knows HTTP get can&amp;rsquo;t encompass streaming-logs functionality&lt;/em>&lt;/p>
&lt;p>Sure it can. When you call &lt;code>kubectl get logs&lt;/code> kube-apiserver transitions the existing socket to &lt;a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">chunked transfer encoding mode&lt;/a>, like any other streaming service. It then interacts with the kubelet(s) running on the pod(s) you want logs from and proxies back the logs to kubectl, which parrots them back to you. You&amp;rsquo;re never talking to anything but kube-apiserver, and you&amp;rsquo;re never speaking any protocol but HTTP.&lt;/p>
&lt;p>&lt;em>Well that&amp;rsquo;s obvious bullcrap Dave&lt;/em> I can hear you say. &lt;em>I can kubectl exec to a pod like remote-login and http get can&amp;rsquo;t encompass full terminal functionality&lt;/em>&lt;/p>
&lt;p>Of course it can. When you call &lt;code>kubectl exec&lt;/code> kube-apiserver merely upgrades the connection to a &lt;a href="https://en.wikipedia.org/wiki/WebSocket">bidirectional websocket&lt;/a>, interacts with the kubelet on the pod to derive a shell, and then cross-wires stdin, stdout, and stderr accordingly. You&amp;rsquo;re never talking to anything but kube-apiserver, and you&amp;rsquo;re never speaking any protocol but HTTP (and websockets).&lt;/p>
&lt;p>&lt;em>What do you mean &lt;strong>interact&lt;/strong> Dave?!&lt;/em> I can hear you say. Fair point. Kubelet has its own API interface, which is immaterial to the point. (Maybe it&amp;rsquo;s impossible to talk about Kubernetes without lying?)&lt;/p>
&lt;p>&lt;em>The frick is a kubelet Dave?!&lt;/em> I can ALSO hear you say. Just&amp;hellip; chill for a sec we&amp;rsquo;ll get to that. What I need you to internalize right now is&amp;hellip;&lt;/p>
&lt;h2 id="there-is-only-one-interface-there-is-only-one-protocol">There is only one interface. There is only one protocol.&lt;/h2>
&lt;p>Kube-apiserver is the one and only interface into the distributed system we describe as &amp;ldquo;Kubernetes&amp;rdquo; and kube-apiserver only speaks HTTP verbs like GET, PATCH, POST and DELETE. That&amp;rsquo;s kinda weird I grant you, but realize how acutely it simplifies things. It means, for example, that &lt;strong>all role-based access control lives in one place: kube-apiserver&lt;/strong>.&lt;/p>
&lt;p>Because literally &lt;strong>anything&lt;/strong> in the system that wants to interact in any way with &lt;strong>any other&lt;/strong> component of the system, does so by traversing kube-apiserver, this shared endpoint handily becomes the single point of authorization in the system. All RBAC is implemented on HTTP requests to kube-apiserver. If you, for example, want to GET Pods in the &lt;code>secret-pods&lt;/code> namespace, the req will be directed at kube-apiserver, which checks your RBAC. But if a process running in Kubernetes wants to modify the &lt;code>status&lt;/code> field of another process in a different Kubernetes namespace, &lt;em>the exact same thing happens&lt;/em>, it directs a request to kube-apiserver, which checks its RBAC. Easy peasy.&lt;/p>
&lt;p>Hey check that out! Now you have a high-level mental model for how Kubernetes role-based access control works, and lemme tell ya, &lt;em>NOBODY has a mental model of the architecture of Kubernetes role-based access control&lt;/em>. You&amp;rsquo;re already killing it.&lt;/p>
&lt;h2 id="so-where-do-the-urls-come-from">So where do the URLs come from?&lt;/h2>
&lt;p>Lets take another look at the endpoint address kubectl directed our HTTP GET request to when we listed pods earlier. You&amp;rsquo;ll notice that its directed to this very normal and not at all magical looking URL:&lt;/p>
&lt;p>&lt;code>GET /api/v1/namespaces/foo/pods HTTP/1.1&lt;/code>&lt;/p>
&lt;p>It breaks down obviously. We&amp;rsquo;re &lt;code>GET&lt;/code>ing &lt;code>pod&lt;/code>s from the namespace called &lt;code>foo&lt;/code>. In normal-land, you&amp;rsquo;d describe this as an &lt;em>API endpoint&lt;/em> but the Yealots call this a &lt;code>resource&lt;/code>.&lt;/p>
&lt;p>Hold on Dave, what makes it a &lt;em>resource&lt;/em> and not an API endpoint you ask?&lt;/p>
&lt;p>Nothing whatsoever. The Yealots just don&amp;rsquo;t want you to know that a Kubernetes resource is just an API endpoint. I&amp;rsquo;ll say that one more time; &lt;strong>A Kubernetes resource is just an API endpoint&lt;/strong>. They made up a word for it to confuse &lt;em>you&lt;/em> specifically.&lt;/p>
&lt;p>Lets look at another API-endpoint&amp;ndash; er I mean &lt;em>resource&lt;/em>. This time, instead of pods, we&amp;rsquo;ll take a look at ssl certificates.&lt;/p>
&lt;p>&lt;code>GET /apis/cert-manager.io/v1/namespaces/foo/certificates&lt;/code>&lt;/p>
&lt;p>The first thing I&amp;rsquo;d like to point out about this resource is the distinction between &lt;code>/api&lt;/code> for &lt;code>pods&lt;/code> and &lt;code>/apis&lt;/code> for &lt;code>certificates&lt;/code>. This difference in the API path denotes endpoints that are &amp;ldquo;&lt;em>core&lt;/em>&amp;rdquo; (bundled together with kube-apiserver), and those that are &amp;ldquo;&lt;em>custom&lt;/em>&amp;rdquo; (installed by platform engineers as extensions to the API after the fact).&lt;/p>
&lt;p>So you can &lt;em>extend the API&lt;/em> with new resources, which is &lt;strong>the&lt;/strong> fundamental feature of kube-apiserver. You can make new endpoints&amp;ndash; er&amp;hellip; resources. When we do this &amp;ndash; when we add new resources to kube-apiserver &amp;ndash; the yealots call them: &lt;strong>Custom Resources&lt;/strong>. This is a &lt;em>really big deal&lt;/em> in Yealot land &amp;ndash; &lt;em>prime&lt;/em> yealot verbiage &amp;ndash; they &lt;strong>love&lt;/strong> blathering on about &lt;em>custom resources&lt;/em>, even despite their own unwillingness (or inability?) to define wtf custom resources are. But now &lt;strong>you&lt;/strong> of all people, a Kubernetes hater and enemy of the dogma, know what &lt;em>custom resources&lt;/em> are concretely. They&amp;rsquo;re just API endpoints. And more specifically API &lt;em>extensions&lt;/em>. They&amp;rsquo;re API endpoints we had to teach kube-apiserver about after the fact.&lt;/p>
&lt;p>Then in the URL we have &lt;code>cert-manager.io/v1&lt;/code> which is what the Yealots call a &amp;ldquo;GroupVersion&amp;rdquo;. Because it&amp;rsquo;s composed of a group &lt;code>cert-manager.io&lt;/code> and a version &lt;code>v1&lt;/code>. Together with the &lt;code>certificates&lt;/code> moniker at the end of the path, you have what the Yealots call a &lt;em>GroupVersionKind&lt;/em> or GVK, which which is a sort of UUID the Yealots use to uniquely identify resources in Kubernetes. Why not use the literal &lt;em>universal resource locator&lt;/em>? No idea.&lt;/p>
&lt;p>Quick Pro-tip: Yealots will often speak of &lt;code>kind&lt;/code>s, expecting you to understand that the word, when they speak it, specifically refers to a &lt;code>GVK&lt;/code> when the word itself also carries a very commonly understood definition in engineering vernacular that predates Kubernetes. Prime examples include &lt;em>deployment&lt;/em> to specifically mean an instance of an &lt;code>apps/v1 deployment&lt;/code>, and &lt;em>application&lt;/em> to specifically mean an instance of an &lt;code>argoprojio/v1alpha1 Application&lt;/code>. Expecting you to intuit whether they&amp;rsquo;re talking about a Go type, internal to k8s or not is just one of their fun little quirks, but you can usually spot this one because when they do it, their tone switches from patronizing to &lt;em>extremely patronizing&lt;/em>.&lt;/p>
&lt;p>If you already have some experience squinting at Kubernetes YAML manifests, you may have seen these same GroupVersionKind values listed in YAML manifests as the &lt;code>apiVersion:&lt;/code> and &lt;code>kind:&lt;/code>. Why do the Yealots &lt;strong>write&lt;/strong> these as apiVersion but &lt;strong>speak&lt;/strong> them as GroupVersion? Because &lt;em>screw you specifically&lt;/em>. Nah just kidding, there&amp;rsquo;s an actual (super fun) reason for this that most yealots don&amp;rsquo;t even know about.&lt;/p>
&lt;p>In our present example, the GVK of the certificate resource is &lt;code>cert-manager.io/v1 certificate&lt;/code>. By comparison, another hint that &lt;code>pods&lt;/code> are core resources is the fact they don&amp;rsquo;t have a group. Not in their endpoint URL, nor in their GVK. They&amp;rsquo;re just &lt;code>v1/pod&lt;/code> (internally to the runtime libs, their group value is the empty string value &lt;code>&amp;quot;&amp;quot;&lt;/code>). You might think this is intentional, but no, it&amp;rsquo;s happenstance. The resources in the &lt;code>&amp;quot;&amp;quot;&lt;/code> group (called the &lt;em>core&lt;/em> or sometimes &lt;em>legacy&lt;/em> group in the k8s runtime libraries) predate the &lt;em>concept&lt;/em> of groups in the k8s runtime. Initially, &lt;em>everything&lt;/em> was just a version/kind, represented by the &lt;code>apiVersion&lt;/code> and &lt;code>kind&lt;/code> fields in YAML. It was called &lt;code>apiVersion&lt;/code> because &lt;em>group didn&amp;rsquo;t exist yet&lt;/em>.&lt;/p>
&lt;p>When the project figured out there were eventually going to be a shitload of apiVersion/kinds, they came up with the concept of &lt;em>groups&lt;/em> as a means of adding some scope to the name-space. So now we can have a &lt;code>confusing.io/v1 pod&lt;/code> AND a &lt;code>stillconfusing.io/v1&lt;/code> pod without name-collision. That&amp;rsquo;s cool, but then, rather than extend the schema for the &lt;code>group&lt;/code> moniker, they just &lt;em>wedged the group name&lt;/em> into the pre-existing apiVersion field and pretended that was acceptable for you and the rest of the world to just &lt;em>deal with&lt;/em>.&lt;/p>
&lt;p>Fun fact: There&amp;rsquo;s &lt;em>yet another&lt;/em> category of group which ships with kube-apiserver, but is not part of the &lt;code>&amp;quot;&amp;quot;&lt;/code> group. Sort of a non-legacy, built-in category. &lt;em>All the resources we added to core after we came up with the concept of groups so we gave them a group instead of adding to core&lt;/em>. These ship as part of kube-apiserver and there&amp;rsquo;s no real way to tell them apart from custom resources, other than to just &lt;strong>know&lt;/strong>. Yealots love that kind of shit. We&amp;rsquo;ll see one of these resources in action in a bit.&lt;/p>
&lt;h2 id="how-do-we-add-new-endpoi---er-resources">How do we add new endpoi&amp;ndash; er.. resources?&lt;/h2>
&lt;p>Ok, lets review. A &lt;em>resource&lt;/em> is just an api endpoint URL on kube-apiserver. &lt;em>Custom&lt;/em> ones are added to kube-apiserver after install, but there are a bunch that kube-apiserver already knows about. Some have groups, others don&amp;rsquo;t.&lt;/p>
&lt;p>So how do we go about adding a custom resource? With a &lt;em>CustomResourceDefinition&lt;/em> GVK. CRD&amp;rsquo;s (the only kind ever abbreviated in conversation for some reason) are another &lt;strong>great big deal&lt;/strong> among the Yealots. They love, &lt;em>love&lt;/em>, &lt;strong>love&lt;/strong> talking about CRDs, but to remain as confusing as possible, they often conflate the terms &lt;em>custom resource&lt;/em> and &lt;em>CRD&lt;/em> to describe a &lt;em>thingy&lt;/em> on the &lt;em>Kubernetes server&lt;/em>. But YOU know the truth: There is no Kubernetes server, a &lt;em>custom resource&lt;/em> is an API endpoint on kube-apiserver, and a &lt;em>CRD&lt;/em> is what we use to install a new &lt;em>custom resource&lt;/em>. CRDs install Custom Resources. They aren&amp;rsquo;t the same thing. Lets take a look at a CRD:&lt;/p>
&lt;div class="highlight">&lt;pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">&lt;span style="color:#f92672">apiVersion&lt;/span>: &lt;span style="color:#ae81ff">apiextensions.k8s.io/v1&lt;/span>
&lt;span style="color:#f92672">kind&lt;/span>: &lt;span style="color:#ae81ff">CustomResourceDefinition&lt;/span>
&lt;span style="color:#f92672">metadata&lt;/span>:
&lt;span style="color:#f92672">name&lt;/span>: &lt;span style="color:#ae81ff">certificates.cert-manager.io&lt;/span>
&lt;span style="color:#f92672">spec&lt;/span>:
&lt;span style="color:#f92672">group&lt;/span>: &lt;span style="color:#ae81ff">cert-manager.io&lt;/span>
&lt;span style="color:#f92672">names&lt;/span>:
&lt;span style="color:#f92672">kind&lt;/span>: &lt;span style="color:#ae81ff">Certificate&lt;/span>
&lt;span style="color:#f92672">listKind&lt;/span>: &lt;span style="color:#ae81ff">CertificateList&lt;/span>
&lt;span style="color:#f92672">plural&lt;/span>: &lt;span style="color:#ae81ff">certificates&lt;/span>
&lt;span style="color:#f92672">singular&lt;/span>: &lt;span style="color:#ae81ff">certificate&lt;/span>
&lt;span style="color:#f92672">shortNames&lt;/span>:
- &lt;span style="color:#ae81ff">cert&lt;/span>
- &lt;span style="color:#ae81ff">certs&lt;/span>
&lt;span style="color:#f92672">categories&lt;/span>:
- &lt;span style="color:#ae81ff">cert-manager&lt;/span>
&lt;span style="color:#f92672">scope&lt;/span>: &lt;span style="color:#ae81ff">Namespaced&lt;/span>
&lt;span style="color:#f92672">versions&lt;/span>:
- &lt;span style="color:#f92672">name&lt;/span>: &lt;span style="color:#ae81ff">v1&lt;/span>
&lt;span style="color:#f92672">served&lt;/span>: &lt;span style="color:#66d9ef">true&lt;/span>
&lt;span style="color:#f92672">storage&lt;/span>: &lt;span style="color:#66d9ef">true&lt;/span>
&lt;span style="color:#f92672">subresources&lt;/span>:
&lt;span style="color:#f92672">status&lt;/span>: {}
&lt;span style="color:#f92672">schema&lt;/span>:
&lt;span style="color:#f92672">openAPIV3Schema&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">object&lt;/span>
&lt;span style="color:#f92672">properties&lt;/span>:
&lt;span style="color:#f92672">spec&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">object&lt;/span>
&lt;span style="color:#f92672">required&lt;/span>:
- &lt;span style="color:#ae81ff">secretName&lt;/span>
- &lt;span style="color:#ae81ff">issuerRef&lt;/span>
&lt;span style="color:#f92672">properties&lt;/span>:
&lt;span style="color:#f92672">secretName&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">string&lt;/span>
&lt;span style="color:#f92672">dnsNames&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">array&lt;/span>
&lt;span style="color:#f92672">items&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">string&lt;/span>
&lt;span style="color:#f92672">issuerRef&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">object&lt;/span>
&lt;span style="color:#f92672">properties&lt;/span>:
&lt;span style="color:#f92672">name&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">string&lt;/span>
&lt;span style="color:#f92672">kind&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">string&lt;/span>
&lt;span style="color:#75715e"># ...the real one runs several hundred lines longer, trimmed for sanity&lt;/span>
&lt;span style="color:#f92672">status&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">object&lt;/span>
&lt;span style="color:#f92672">properties&lt;/span>:
&lt;span style="color:#f92672">conditions&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">array&lt;/span>
&lt;span style="color:#f92672">items&lt;/span>:
&lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">object&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Notice the GVK here: &lt;code>apiextensions.k8s.io/v1 CustomResourceDefinition&lt;/code>. This is one of the groups I mentioned earlier that have a proper name, but are built-in and ship with kube-apiserver. So we can surmise from the GVK that &lt;code>/apis/apiextensions.k8s.io/v1/CustomResourceDefinition&lt;/code> is the resource endpoint we send HTTP POST&amp;rsquo;s to when we want to extend the kube-apiserver with a new endpoint&amp;ndash; er.. resource. Note that URL isn&amp;rsquo;t namespaced; new CRDs are applied &lt;em>cluster wide&lt;/em>.&lt;/p>
&lt;p>If you squint at it, you&amp;rsquo;ll see the CRD is essentially a resource manifest, with another resource manifest nested in its &lt;code>spec&lt;/code> field, including the typed schema for the child kind (in this case, a certificate kind). This is how CRDs work, they describe new kinds and the schema kube-apiserver needs to persist instances of them.&lt;/p>
&lt;p>When you &lt;em>apply this manifest to Kubernetes&lt;/em> as the Yealots say (but which you now know actually means &lt;em>POST it to the kube-apiserver CustomResourceDefinition endpoint&lt;/em>) kube-apiserver responds by internally re-wiring itself to respond on the shiny new certificates URL:&lt;/p>
&lt;p>&lt;code>/apis/cert-manager.io/v1/namespaces/&amp;lt;namespace&amp;gt;/certificates&lt;/code>&lt;/p>
&lt;h2 id="state-gets-stored-in-etcd">State gets stored in etcd&lt;/h2>
&lt;p>Part of kube-apiserver&amp;rsquo;s process of re-wiring itself, includes storing a copy of all CRDs as entries in its underlying etcd database. This enables kube-apiserver to rebuild it&amp;rsquo;s API surface between restarts. In fact, kube-apiserver stores every modification to every resource in etcd. So after the CRD is applied and the new &lt;code>certificates&lt;/code> endpoint is live&amp;hellip;&lt;/p>
&lt;ul>
&lt;li>kube-apiserver has a copy of the &lt;code>CustomResourceDefinition&lt;/code> defining the &lt;code>certificate&lt;/code> kind so it&amp;rsquo;s a permanent extension to the API.&lt;/li>
&lt;/ul>
&lt;ul>
&lt;li>When instances of the new &lt;em>certificate&lt;/em> kind get &lt;code>POST&lt;/code>ed to the new certificates endpoint, kube-apiserver writes them to etcd.&lt;/li>
&lt;li>When &lt;code>GET&lt;/code> requests are made to the certificates endpoint, kube-apiserver reads all the matching certificates records from etcd and spits them out as HTTP response body.&lt;/li>
&lt;li>When modifications to already existing certificates are &lt;code>PATCH&lt;/code>ed to the certificates endpoint, kube-apiserver finds the instance referred to by the HTTP request in etcd, modifies it in the way described in the patch, and writes the result back to etcd.&lt;/li>
&lt;li>When &lt;code>DELETE&lt;/code> requests are sent to the new certificates endpoint, kube-apiserver finds the certificate record referred to by the HTTP request in etcd, and deletes it.&lt;/li>
&lt;li>If someone comes along and deletes the &lt;code>certificate&lt;/code> CRD itself, kube-apiserver deletes the resource and ensuing requests to it will 404.&lt;/li>
&lt;/ul>
&lt;p>I&amp;rsquo;m over-simplifying a bit there. Operations like GET don&amp;rsquo;t necessarily result in an etcd operation, the cacheing you would expect to be there is there.&lt;/p>
&lt;p>The frick do you mean &lt;em>an instances of the new certificate kind?!&lt;/em> Dave?&lt;/p>
&lt;p>The entire point of extending kube-apiserver with new custom resources is to get it to perform CRUD operations on stored &lt;em>instances&lt;/em> of the new resource kind in etcd. By &lt;em>instance of the kind&lt;/em> I mean a data record with the shape we defined in the CRD spec, which has all the required values filled in. Here&amp;rsquo;s an example of an instance of the &lt;code>certificate&lt;/code> kind:&lt;/p>
&lt;div class="highlight">&lt;pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">&lt;span style="color:#f92672">apiVersion&lt;/span>: &lt;span style="color:#ae81ff">cert-manager.io/v1&lt;/span>
&lt;span style="color:#f92672">kind&lt;/span>: &lt;span style="color:#ae81ff">Certificate&lt;/span>
&lt;span style="color:#f92672">metadata&lt;/span>:
&lt;span style="color:#f92672">name&lt;/span>: &lt;span style="color:#ae81ff">foo&lt;/span>
&lt;span style="color:#f92672">namespace&lt;/span>: &lt;span style="color:#ae81ff">foo&lt;/span>
&lt;span style="color:#f92672">spec&lt;/span>:
&lt;span style="color:#f92672">secretName&lt;/span>: &lt;span style="color:#ae81ff">foo-tls&lt;/span>
&lt;span style="color:#f92672">dnsNames&lt;/span>:
- &lt;span style="color:#ae81ff">foo.example.com&lt;/span>
&lt;span style="color:#f92672">issuerRef&lt;/span>:
&lt;span style="color:#f92672">name&lt;/span>: &lt;span style="color:#ae81ff">foo-issuer&lt;/span>
&lt;span style="color:#f92672">kind&lt;/span>: &lt;span style="color:#ae81ff">Issuer&lt;/span>
&lt;span style="color:#f92672">status&lt;/span>:
&lt;span style="color:#f92672">conditions&lt;/span>:
- &lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">Ready&lt;/span>
&lt;span style="color:#f92672">status&lt;/span>: &lt;span style="color:#e6db74">&amp;#34;True&amp;#34;&lt;/span>
&lt;span style="color:#f92672">reason&lt;/span>: &lt;span style="color:#ae81ff">Ready&lt;/span>
&lt;span style="color:#f92672">message&lt;/span>: &lt;span style="color:#ae81ff">Certificate is up to date and has not expired&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>If you compare this yaml manifest to the CRD above (squinting hard enough), you&amp;rsquo;ll see that this manifest, &lt;em>manifests&lt;/em> the shape described by the CRD above. If you POST this to the certificates endpoint, kube-apiserver will deserialize it into a new instance of the certificate kind, named foo in the namespace foo with the rest of the settings detailed above, and save it to etcd. If you HTTP GET certificates, you&amp;rsquo;ll get a list of these.&lt;/p>
&lt;p>As far as I can tell, YAML blobs like this one are essentially the Yealot cognitive model for all resources. This is what they see in their mind when you say &lt;em>pod&lt;/em> &amp;ndash; a YAML manifest. But really, inside Kubernetes, the true nature of every resource is a Go Struct serialized into etcd for storage. The YAML version we humans are forced to deal with is just a temporary exported expression of it &amp;ndash; just the &lt;em>human user-interface&lt;/em>. I could pause here to marvel at the hubris of a system that expects you to use serialized copies of its internal data-structures as your UI, while incessantly lying to you about the shape of its architecture, but I&amp;rsquo;m far too grown up and principled for that sort of thing.&lt;/p>
&lt;p>ANYWAY. The tl;dr is: This is how &lt;strong>all things are modeled&lt;/strong> in Kubernetes. Everything Kubernetes knows how to deal with &amp;ndash; every object it models &amp;ndash; pods, deployments, certificates, even extensions to its own API surface. They are all dedicated URL endpoints (called &lt;em>resources&lt;/em>) on kube-apiserver with a schema defined by an associated &lt;em>CRD&lt;/em> (or a built-in one); their current state all backed by etcd. All intercommunication between Kubernetes entities including human interaction consist of HTTP gets, posts, patches and etc&amp;hellip; to the kube-apiserver. Every operation is just an HTTP req against this single endpoint that defines, creates, reads, updates, or deletes resources.&lt;/p>
&lt;h2 id="but-how-does-it-know-how-to-work-with-new-types-it-just-learned">But how does it know how to work with new types it just learned?&lt;/h2>
&lt;p>Feel free to skip this little section if you aren&amp;rsquo;t a Go programmer, but if you are a Go progammer, it will occur to you that inside kube-apiserver, the native format of all of these kinds are Go types. Typically, Go programs need to re-compile to learn new types. So how can we extend the API surface of kube-apiserver in runtime without a restart?&lt;/p>
&lt;p>The answer is the &lt;a href="https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1/unstructured">unstructured&lt;/a> package. The short answer here is that kube-apiserver, along with the controllers assisting it, pack custom resources into unstructured.Unstructured types, which in turn implement the &lt;a href="https://pkg.go.dev/k8s.io/apimachinery/pkg/runtime#Object">runtime.Object&lt;/a> interface, which in turn enables us to pass these new types around and strongly-type them later inside a controller that&amp;rsquo;s compiled with types to ingest and work with the GVK (we&amp;rsquo;re getting to controllers in a bit). The &lt;code>unstructured&lt;/code> envelope is where metadata.name et..al come from in the instance above despite not being in the CRD. Meanwhile, etcd, the persistence layer underlying all this is untyped by design, so it doesn&amp;rsquo;t need schema.&lt;/p>
&lt;h2 id="well-wtf-is-the-point-of-that">Well WTF is the point of that?&lt;/h2>
&lt;p>If you feel like that all seems a bit masturbatory you&amp;rsquo;re on the right page. What the hell is the point of just writing records to etcd?! How does &lt;strong>that&lt;/strong> become deploying containers, and allocating hardware, and stateful sets, and rollouts, and you know&amp;hellip; &lt;em>Kubernetes&lt;/em>?&lt;/p>
&lt;p>Let me show you something cool (or maybe gross depending on your REST orthodoxy) about kube-apiserver. I actually teased it a bit earlier in this article, when I gave you the example of running &lt;code>kubectl get logs&lt;/code>, which resulted in kube-apiserver transitioning the connection to chunked streaming mode.&lt;/p>
&lt;p>It turns out? You can do that with &lt;strong>every resource&lt;/strong>. For example:&lt;/p>
&lt;p>&lt;code>GET /api/v1/namespaces/foo/pods?watch=true&lt;/code>&lt;/p>
&lt;p>You&amp;rsquo;ll notice I&amp;rsquo;ve tacked on the URL parameter &lt;code>watch=true&lt;/code> to this &lt;em>get pods&lt;/em> request for pods in the &lt;code>foo&lt;/code> namespace. You can tack this on to any HTTP GET request targeted at any endpoint&amp;ndash; er resource, and kube-apiserver will respond by transitioning to streaming mode. Your client will block for eternity, kube-apiserver has no means of disconnecting you. On the wire you&amp;rsquo;ll begin receiving updates for every operation that happens to the resource, or group of resources that match your GET request &lt;em>as they happen in real-time&lt;/em>. They look like this:&lt;/p>
&lt;div class="highlight">&lt;pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-json" data-lang="json">{&lt;span style="color:#f92672">&amp;#34;type&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;ADDED&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;object&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;apiVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;v1&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;kind&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Pod&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;metadata&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;namespace&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;resourceVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;10234&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;uid&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;a1b2c3d4-...&amp;#34;&lt;/span>},&lt;span style="color:#f92672">&amp;#34;spec&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;containers&amp;#34;&lt;/span>:[{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;image&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;nginx:1.27&amp;#34;&lt;/span>}]},&lt;span style="color:#f92672">&amp;#34;status&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;phase&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Pending&amp;#34;&lt;/span>}}}
{&lt;span style="color:#f92672">&amp;#34;type&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;MODIFIED&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;object&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;apiVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;v1&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;kind&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Pod&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;metadata&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;namespace&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;resourceVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;10236&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;uid&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;a1b2c3d4-...&amp;#34;&lt;/span>},&lt;span style="color:#f92672">&amp;#34;spec&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;containers&amp;#34;&lt;/span>:[{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;image&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;nginx:1.27&amp;#34;&lt;/span>}],&lt;span style="color:#f92672">&amp;#34;nodeName&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;node-3&amp;#34;&lt;/span>},&lt;span style="color:#f92672">&amp;#34;status&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;phase&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Pending&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;containerStatuses&amp;#34;&lt;/span>:[{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;state&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;waiting&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;reason&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;ContainerCreating&amp;#34;&lt;/span>}},&lt;span style="color:#f92672">&amp;#34;ready&amp;#34;&lt;/span>:&lt;span style="color:#66d9ef">false&lt;/span>,&lt;span style="color:#f92672">&amp;#34;restartCount&amp;#34;&lt;/span>:&lt;span style="color:#ae81ff">0&lt;/span>}]}}}
{&lt;span style="color:#f92672">&amp;#34;type&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;MODIFIED&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;object&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;apiVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;v1&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;kind&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Pod&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;metadata&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;namespace&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;resourceVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;10241&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;uid&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;a1b2c3d4-...&amp;#34;&lt;/span>},&lt;span style="color:#f92672">&amp;#34;spec&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;containers&amp;#34;&lt;/span>:[{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;image&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;nginx:1.27&amp;#34;&lt;/span>}],&lt;span style="color:#f92672">&amp;#34;nodeName&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;node-3&amp;#34;&lt;/span>},&lt;span style="color:#f92672">&amp;#34;status&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;phase&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Running&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;podIP&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;10.244.2.17&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;containerStatuses&amp;#34;&lt;/span>:[{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;state&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;running&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;startedAt&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;2026-09-10T14:02:11Z&amp;#34;&lt;/span>}},&lt;span style="color:#f92672">&amp;#34;ready&amp;#34;&lt;/span>:&lt;span style="color:#66d9ef">true&lt;/span>,&lt;span style="color:#f92672">&amp;#34;restartCount&amp;#34;&lt;/span>:&lt;span style="color:#ae81ff">0&lt;/span>}],&lt;span style="color:#f92672">&amp;#34;conditions&amp;#34;&lt;/span>:[{&lt;span style="color:#f92672">&amp;#34;type&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Ready&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;status&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;True&amp;#34;&lt;/span>}]}}}
{&lt;span style="color:#f92672">&amp;#34;type&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;MODIFIED&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;object&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;apiVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;v1&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;kind&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Pod&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;metadata&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;namespace&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;resourceVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;10309&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;uid&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;a1b2c3d4-...&amp;#34;&lt;/span>},&lt;span style="color:#f92672">&amp;#34;status&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;phase&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Running&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;containerStatuses&amp;#34;&lt;/span>:[{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;state&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;waiting&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;reason&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;CrashLoopBackOff&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;message&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;back-off 40s restarting failed container&amp;#34;&lt;/span>}},&lt;span style="color:#f92672">&amp;#34;ready&amp;#34;&lt;/span>:&lt;span style="color:#66d9ef">false&lt;/span>,&lt;span style="color:#f92672">&amp;#34;restartCount&amp;#34;&lt;/span>:&lt;span style="color:#ae81ff">4&lt;/span>,&lt;span style="color:#f92672">&amp;#34;lastState&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;terminated&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;exitCode&amp;#34;&lt;/span>:&lt;span style="color:#ae81ff">1&lt;/span>,&lt;span style="color:#f92672">&amp;#34;reason&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Error&amp;#34;&lt;/span>}}}],&lt;span style="color:#f92672">&amp;#34;conditions&amp;#34;&lt;/span>:[{&lt;span style="color:#f92672">&amp;#34;type&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Ready&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;status&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;False&amp;#34;&lt;/span>}]}}}
{&lt;span style="color:#f92672">&amp;#34;type&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;BOOKMARK&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;object&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;apiVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;v1&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;kind&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Pod&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;metadata&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;resourceVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;10402&amp;#34;&lt;/span>}}}
{&lt;span style="color:#f92672">&amp;#34;type&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;DELETED&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;object&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;apiVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;v1&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;kind&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Pod&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;metadata&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;name&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;namespace&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;foo&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;resourceVersion&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;10455&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;uid&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;a1b2c3d4-...&amp;#34;&lt;/span>,&lt;span style="color:#f92672">&amp;#34;deletionTimestamp&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;2026-09-10T14:11:03Z&amp;#34;&lt;/span>},&lt;span style="color:#f92672">&amp;#34;status&amp;#34;&lt;/span>:{&lt;span style="color:#f92672">&amp;#34;phase&amp;#34;&lt;/span>:&lt;span style="color:#e6db74">&amp;#34;Running&amp;#34;&lt;/span>}}}
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Every line is a complete JSON object. They&amp;rsquo;re newline-delimited. You&amp;rsquo;d have thought they&amp;rsquo;d use YAML, but in fact most the responses from kube-apiserver are json. If you squint you&amp;rsquo;ll see embedded &lt;code>resourceVersion&lt;/code> and &lt;code>uid&lt;/code> attributes, which are monotonically increasing counters which can be used by client watchers to reconnect to the stream at a named value with eg.. &lt;code>?watch=true&amp;amp;resourceVersion=10309&lt;/code> or disambiguate races. There are similar hacks in place in kube-apiserver to handle race-conditions on operations to etcd. If, for example, two different clients send delete and update the same time.&lt;/p>
&lt;p>So now you have the full picture on kube-apiserver. It listens on a bunch of custom resource URLs for requests. It has a connection to etcd, and an in-memory map of open watch sockets, keyed by GVK/namespace. When a request happens, kube-apiserver services the request, CRUD&amp;rsquo;ing what it needs to/from etcd, and then summarizing what it did for whatever watchers match the GVK/namespace on the resource.&lt;/p>
&lt;p>So how does that help? Woopie-doo, we can watch things.&lt;/p>
&lt;h2 id="enter-controllers">Enter: Controllers&lt;/h2>
&lt;p>Lets imagine we had a computer program that knew how to deal with TLS Certs. This program knows how to do things like take a secret and generate a CSR (signing request) for it.&lt;/p>
&lt;p>Well, we already have a Kubernetes kind that represents a TLS Certificate. It has all the fields we need to store a cert. The issuer ref, the secret, etc&amp;hellip; So we could totally take our computer program that knows how to do things to certs, and we could have it &lt;em>watch&lt;/em> all the instances of the certificate kind stored on the Kubernetes cluster. It could totally:&lt;/p>
&lt;ul>
&lt;li>watch for changes to certificates and regenerate them if the authority changes for example&lt;/li>
&lt;li>search for certs that have a secret but not a proper Cert yet&lt;/li>
&lt;li>wake up every so often and just make sure no certs are about to expire by checking the expiry date&lt;/li>
&lt;li>Other certificate related bullcrap.&lt;/li>
&lt;/ul>
&lt;p>To be useful, our little computer program will need to interact with other cert-related computer programs. For example, if our first computer program encounters a &lt;code>certificate&lt;/code> instance that does not have a ref to a non-expiring cert matching its spec, it could generate a signing request. Maybe we have a kind for it called &lt;code>CertificateRequest&lt;/code>, which looks like this:&lt;/p>
&lt;div class="highlight">&lt;pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">&lt;span style="color:#f92672">apiVersion&lt;/span>: &lt;span style="color:#ae81ff">cert-manager.io/v1&lt;/span>
&lt;span style="color:#f92672">kind&lt;/span>: &lt;span style="color:#ae81ff">CertificateRequest&lt;/span>
&lt;span style="color:#f92672">metadata&lt;/span>:
&lt;span style="color:#f92672">name&lt;/span>: &lt;span style="color:#ae81ff">foo-abc123&lt;/span>
&lt;span style="color:#f92672">namespace&lt;/span>: &lt;span style="color:#ae81ff">foo&lt;/span>
&lt;span style="color:#f92672">ownerReferences&lt;/span>:
- &lt;span style="color:#f92672">apiVersion&lt;/span>: &lt;span style="color:#ae81ff">cert-manager.io/v1&lt;/span>
&lt;span style="color:#f92672">kind&lt;/span>: &lt;span style="color:#ae81ff">Certificate&lt;/span>
&lt;span style="color:#f92672">name&lt;/span>: &lt;span style="color:#ae81ff">foo&lt;/span>
&lt;span style="color:#f92672">uid&lt;/span>: &lt;span style="color:#ae81ff">a1b2c3d4-...&lt;/span>
&lt;span style="color:#f92672">spec&lt;/span>:
&lt;span style="color:#f92672">issuerRef&lt;/span>:
&lt;span style="color:#f92672">name&lt;/span>: &lt;span style="color:#ae81ff">foo-issuer&lt;/span>
&lt;span style="color:#f92672">kind&lt;/span>: &lt;span style="color:#ae81ff">Issuer&lt;/span>
&lt;span style="color:#f92672">request&lt;/span>: &lt;span style="color:#ae81ff">LS0tLS1CRUdJTiBDRVJUSUZJQ0FURSBSRVFVRVNULS0tLS0K...&lt;/span>
&lt;span style="color:#f92672">status&lt;/span>:
&lt;span style="color:#f92672">conditions&lt;/span>:
- &lt;span style="color:#f92672">type&lt;/span>: &lt;span style="color:#ae81ff">Ready&lt;/span>
&lt;span style="color:#f92672">status&lt;/span>: &lt;span style="color:#e6db74">&amp;#34;False&amp;#34;&lt;/span>
&lt;span style="color:#f92672">reason&lt;/span>: &lt;span style="color:#ae81ff">Pending&lt;/span>
&lt;span style="color:#f92672">message&lt;/span>: &lt;span style="color:#ae81ff">Waiting on certificate issuance from order foo-abc123-1&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>We would teach kube-apiserver about &lt;code>CertificateRequest&lt;/code> resources with a CRD, and then, a second computer program &amp;ndash; one that&amp;rsquo;s back-end connected to a cert-authority and knows how to sign requests like this &amp;ndash; could be &lt;code>watch&lt;/code>ing for signing requests like this one. When it sees them, it can take action, signing the request and writing the result as an update directly back to the &lt;code>certificates&lt;/code> resource using an HTTP PATCH to the kube-apiserver endpoint. Our first program, still dutifully watching for updates to &lt;code>certificates&lt;/code> would then pick up the change like a message in a bottle.&lt;/p>
&lt;p>These computer programs, as you may have guessed from the section title, are called &lt;em>controllers&lt;/em>. This is essentially how they all work, watching a resource, responding to changes, communicating their response by writing back to kube-apiserver. In real life, controllers tend to be many-to-one with resources. Every resource tends to have a few controllers watching it. Controllers tend to follow the old Unix design philosophy of &lt;em>do one thing and do it well&lt;/em>, and they don&amp;rsquo;t communicate with each other directly, but via HTTP requests back to kube-apiserver. The &lt;code>cert-manager.io/v1&lt;/code> group in real life, for example, has over 20 controllers operating on 6 distinct resources. Each controller watches one resource, has one job, and communicates via HTTP PATCH requests back to instances of resources on kube-apiserver.&lt;/p>
&lt;p>Controllers are the turtles of this whole &lt;em>Kubernetes&lt;/em> thing. It&amp;rsquo;s just controllers all the way down. Even kube-apiserver runs several of them to deal with CRD&amp;rsquo;s:&lt;/p>
&lt;ul>
&lt;li>crdController — Watches &lt;code>CustomResourceDefinition&lt;/code> and dynamically registers an HTTP route for new GVKs&lt;/li>
&lt;li>establishingController — flips the CRD&amp;rsquo;s Established condition once its route is live and serving.&lt;/li>
&lt;li>namingController — checks for naming conflicts before a route goes live (two CRDs can&amp;rsquo;t claim the same plural name in the same group).&lt;/li>
&lt;li>openapiController — regenerates the /openapi/v2//openapi/v3 docs to include the new schema.&lt;/li>
&lt;li>finalizingController — handles cleanup when a CRD gets deleted&lt;/li>
&lt;/ul>
&lt;p>You can write a k8s controller in any language that can make an HTTP GET request with URL parameter support. You could write one in three lines of bash with cURL:&lt;/p>
&lt;pre>&lt;code> TOKEN=&amp;lt;some token&amp;gt;
SERVER=&amp;lt;kube-apiserver ip addr&amp;gt;
curl -sk -H &amp;quot;Authorization: Bearer $TOKEN&amp;quot; &amp;quot;$SERVER/api/v1/namespaces/foo/pods?watch=true&amp;quot;
&lt;/code>&lt;/pre>&lt;p>Or even a single line with kubectl:&lt;/p>
&lt;p>&lt;code>kubectl get --raw &amp;quot;/api/v1/namespaces/foo/pods?watch=true&amp;quot;&lt;/code>&lt;/p>
&lt;p>Pipe that to whatever you want, and &lt;strong>POOF&lt;/strong>: Kubernetes controller. Check you out, writing Kubernetes controllers already.&lt;/p>
&lt;h2 id="more-stuff-that-doesnt-exist">More stuff that doesn&amp;rsquo;t exist.&lt;/h2>
&lt;p>Now is a good time to talk about some other imaginary stuff that feature heavily in Yealot folklore. For example, without ever being asked, the Yealots will pontificate at length about both the &lt;code>reconcillation loop&lt;/code> and &lt;code>operators&lt;/code> as if these things exist as first-class concrete features in &amp;ldquo;Kubernetes&amp;rdquo;. These, they will &lt;em>absolutely insist&lt;/em> are baked into the very fabric of.. uh. Ya know&amp;hellip; &lt;strong>IT&lt;/strong>&amp;hellip; &lt;em>Kubernetes&lt;/em>.&lt;/p>
&lt;p>More lies my friend. As you know, Kubernetes doesn&amp;rsquo;t exist, and things that don&amp;rsquo;t exist can&amp;rsquo;t have features, so lets unpack these operator and reconcillation loop (aka control loop) fairytales, because you&amp;rsquo;re gonna have to deal with them (or at least yealots blathering about them).&lt;/p>
&lt;h2 id="the-reconcillation-aka-control-loop-isnt-enforced">The reconcillation (aka control) loop isn&amp;rsquo;t enforced.&lt;/h2>
&lt;p>Most controllers, as you would expect, are not written in bash, but rather in Go. This is because the Kubernetes runtime libs are all written in Go, so the upstream definitions for every resource exist concretely in some other golang package upstream of you. So why reinvent a &lt;code>pod&lt;/code> struct that may drift from the upstream definition over time, when you can just &lt;a href="https://pkg.go.dev/k8s.io/api/core/v1#Pod">import the same definition k8s itself uses&lt;/a>.&lt;/p>
&lt;p>Because most controllers are written in Go, there&amp;rsquo;s an officially-blessed Golang library for writing controllers called &lt;a href="https://pkg.go.dev/sigs.k8s.io/controller-runtime">controller-runtime&lt;/a>. I&amp;rsquo;m over-simplifying here, this landscape is an incomprehensible mess like everything else Kubernetes, code-gen and confusing code markers feature heavily, and there are actually myriad semi-blessed ways to write controllers. I&amp;rsquo;d need a follow-up post to do it all justice, but most controller-writing frameworks import &lt;code>controller-runtime&lt;/code>, so it&amp;rsquo;s something like the lowest-common denominator.&lt;/p>
&lt;p>Controller runtime, in its turn, imports and wraps a very convenient Kubernetes client called &lt;a href="https://github.com/Kubernetes/client-go">&lt;code>client-go&lt;/code>&lt;/a>, which knows how to do things like watch resources without you needing to worry about HTTP clients, bearer tokens, and json serialization. Together client-go and controller-runtime make a contract with you, the controller-author, and &lt;em>THAT contract&lt;/em> looks something like this:&lt;/p>
&lt;p>&lt;em>Tell me what you want to watch and give me a function to run, and I will watch that thing, and run your function when stuff happens to that thing. Also, I&amp;rsquo;ll run your function every few minutes regardless of whether something happens or not, just for kicks.&lt;/em>&lt;/p>
&lt;p>Now, when you create a controller using &lt;code>controller-runtime&lt;/code>, the generally accepted &amp;ldquo;correct&amp;rdquo; pattern &amp;ndash; the guts of the function you provide to the client &amp;ndash; looks &lt;strong>something&lt;/strong> like this:&lt;/p>
&lt;p>&lt;em>Look at the spec field on the struct passed to us by controller-runtime. Compare it to the status field of the same struct. Are the values of those fields the same? If not, fix it.&lt;/em>&lt;/p>
&lt;p>When a controller follows this pattern, it is said to &lt;em>reconcile&lt;/em> the resource. Marrying the spec (desired outcome as described by the literal &lt;code>spec&lt;/code> field) to the state (real world status as depicted by the &lt;code>status&lt;/code> field). Because it does this and only this, in a loop, forever, this pattern is generally called the &lt;em>reconcile loop&lt;/em>.&lt;/p>
&lt;p>This whole-ass stack of assumptions: That you&amp;rsquo;re writing in Go (or some language with analogous k8s-libs), that you are using &lt;code>controller-runtime&lt;/code> and &lt;code>client-go&lt;/code> (or libs with analogous contracts), that the resource in question has something updating its status over time for you (updating status in your function is a no-no), that you have provided a function for a kind that even cares about spec and status etc.. etc.. That whole assumption-sandwitch is what the Yealots are referring to when they speak of the &lt;em>Kubernetes Reconcillation loop&lt;/em> as if it&amp;rsquo;s a written-in-stone thing &lt;em>Kubernetes does&lt;/em>.&lt;/p>
&lt;p>It&amp;rsquo;s not. Reconciliation is entirely implemented in runtime lib as set of optional features provided by the lib, combined with idiomatic patterns to which the community of controller writers generally aspires to adhere. Most controllers do, but plenty do not, by virtue of the fact they happen to do things that simply &lt;em>don&amp;rsquo;t fit the pattern&lt;/em>.&lt;/p>
&lt;p>And, as you&amp;rsquo;ve seen, we can write a controller in any language, completely ignoring &lt;code>controller-runtime&lt;/code> and its other-language-analogous libraries, and the controller we write may do &lt;strong>anything we want&lt;/strong>. To the moon Alice; the Yealots can&amp;rsquo;t stop us. A controller is nothing but a computer program that watches a resource and takes action. Maybe our controller writes back to kube-apiserver, maybe not, but there is no such thing as an enforced first-class feature of &amp;ldquo;Kubernetes&amp;rdquo; called the &amp;ldquo;reconciliation loop&amp;rdquo;.&lt;/p>
&lt;h2 id="operators-dont-exist">Operators don&amp;rsquo;t exist&lt;/h2>
&lt;p>The reconciliation loop, it could be argued, &lt;em>kinda exists&lt;/em> in the form of an idiomatic pattern that&amp;rsquo;s implemented by core libraries, but Operators by comparison are outright vaporware. The Yealots will assert Operators exist as either a controller-like entity that is distinct in some unspecifiable way from a controller, or a higher-level controller-managing entity/thing, which orchestrates controllers together in ways that specifically enable the maintenance of &lt;em>applications&lt;/em> (by which they do NOT mean &lt;em>application kinds&lt;/em>). Sometimes Yealots will assert the former, and when proven wrong, proceed to assert the latter when unfortunately, neither of these things are the truth. The truth is: Operators &lt;em>straight up don&amp;rsquo;t exist&lt;/em>. This is how they&amp;rsquo;re described in the &lt;a href="https://Kubernetes.io/docs/concepts/extend-Kubernetes/operator/">the official documentation&lt;/a>:&lt;/p>
&lt;pre>&lt;code>Operators are software extensions to Kubernetes that make use of custom resources to manage applications and their components. Operators follow Kubernetes principles, notably the control loop.
&lt;/code>&lt;/pre>&lt;p>&lt;em>Software Extensions&lt;/em>. Obviously not controllers right? I mean, if they were just controllers, then the docs would just &lt;em>call them controllers&lt;/em> right?&lt;/p>
&lt;p>Lies my friend. I invite you to sally forth and examine any project that describes itself as an &amp;ldquo;operator&amp;rdquo;. Check out the &lt;a href="https://github.com/external-secrets/external-secrets">external secrets operator&lt;/a> for example. Spoiler alert; what you&amp;rsquo;ll invariably find is a bunch of &lt;a href="https://github.com/external-secrets/external-secrets/tree/main/pkg/controllers">controllers, which are usually in a directory literally named &amp;ldquo;controllers&amp;rdquo;&lt;/a>. If you choose at random, any one of these controllers that are in the controllers directory &amp;ndash; their name suffixed with &lt;code>_controller.go&lt;/code> &amp;ndash; what you&amp;rsquo;ll find is that they &lt;a href="https://github.com/external-secrets/external-secrets/blob/main/pkg/controllers/clusterexternalsecret/clusterexternalsecret_controller.go#L38-L43">import controller-runtime libs&lt;/a> and proceed to use them to &lt;a href="https://github.com/external-secrets/external-secrets/blob/main/pkg/controllers/clusterexternalsecret/clusterexternalsecret_controller.go#L77-L96">voluntarily implement the reconcile pattern&lt;/a>. Operators don&amp;rsquo;t exist. Ya can&amp;rsquo;t make this stuff up. Er.. well I suppose you can, I mean the Yealots totally did.&lt;/p>
&lt;p>The crushing irony of this myth is that there &lt;em>is actually a controller-manager package with a &lt;a href="https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/manager#Manager">manager type&lt;/a>&lt;/em> in the controller-runtime lib, which the Yealots &lt;em>never&lt;/em> talk about. That&amp;rsquo;s a real piece of code that exists; &lt;code>ctrl.Manager&lt;/code>. When I said earlier that there were 20-plus controllers in the &lt;code>cert-manager.io&lt;/code> group, all of those controllers are part of a single process (pod), running beneath a controller manager. So why is external secrets an &lt;em>operator&lt;/em> and cert-manager not? Because the former asserts &lt;em>operator&lt;/em> in its name. Some people call them both operators.&lt;/p>
&lt;p>This &lt;em>Operator&lt;/em> crap sincerely confounds me (if you didn&amp;rsquo;t pick up on the salt). I really have no idea what purpose this fairytale serves, nor can I imagine why it is so pervasive in the community to the extent of having massive k8s projects that &lt;em>call themselves operators&lt;/em>. Especially when a simple, concrete understanding of controllers, which you now posses after like 7 minutes of reading &lt;em>fully describes&lt;/em> the functionality that&amp;rsquo;s repeatedly asserted as belonging to these imaginary &amp;ldquo;operators&amp;rdquo;.&lt;/p>
&lt;p>ANYWAY, when the Yealots point you to the &amp;ldquo;foobar operator&amp;rdquo; or whatever, don&amp;rsquo;t believe their bullshit. What you&amp;rsquo;re looking at is nothing but a gaggle of controllers calling itself an operator. They watch a bunch of little resource endpoints and communicate to each other or other cluster components via HTTP requests directed to kube-apiserver.&lt;/p>
&lt;h2 id="and-thats-legit-all-thats-happening-inside-kubernetes">And that&amp;rsquo;s legit ALL that&amp;rsquo;s happening inside Kubernetes.&lt;/h2>
&lt;p>In fact, what I just described to you, &lt;em>a bunch of little computer programs called controllers listening to a bunch of API endpoints called resources and doing whatever the heck they want&lt;/em> is one-hundred percent &lt;strong>all that is happening inside &amp;ldquo;Kubernetes&amp;rdquo;&lt;/strong>.&lt;/p>
&lt;p>The pod controller watches pod resources and does things with pods. The deployment controller watches deployments and does things with deployments. The entire universe of Kubernetes related complexity reduces to this single pattern. You can extend the API to &amp;ldquo;learn&amp;rdquo; about whatever kind of thing you care about, and then write a controller to operate on modifications to it over time. If you wanted to, you could even create an &lt;em>Operator&lt;/em> CRD and apply it to your cluster. Then at least &amp;ldquo;Operators&amp;rdquo; would exist somewhere in the real world.&lt;/p>
&lt;p>ANYWAY.&lt;/p>
&lt;p>In the opening paragraphs I described Kubernetes as a strangely shaped microservices architecture that takes just about the opposite shape its name implies. I sincerely hope you now have the true shape of it in mind. The single pattern by which all things in Kubernetes work, is controllers watching resources and doing whatever they want. All the esoterica that remains are details inherent to one controller or another. And the preponderance of &lt;em>that&lt;/em> it is jargon made up by a controller to obfuscate some otherwise simple concept you probably already have a solid cognitive model for. Don&amp;rsquo;t get me wrong, controllers do some crazy stuff, but now we have the foundation necessary to begin unraveling the rest of the landscape. A straightforward path for &amp;ldquo;learning Kubernetes&amp;rdquo; laid out now before us: Controller by controller.&lt;/p>
&lt;p>Although I&amp;rsquo;m unsure I have the stomache for it, I&amp;rsquo;d like to follow-through on that strategy, with an exploration of kubelet, which is one of the most complex controllers. Kubelet listens for changes to &amp;lsquo;v1/pods&amp;rsquo; and reconciles workload scheduling (running containers on hardware). This gets pretty interesting because kube-apiserver can&amp;rsquo;t run without kubelet, and yet, kubelet can&amp;rsquo;t &lt;em>do anything&lt;/em> without kube-apiserver. A bootstrapping hack called &amp;ldquo;static pods&amp;rdquo; solves this. Once you understand kubelet you&amp;rsquo;ll also understand &lt;code>pods&lt;/code> (Kubernetes&amp;rsquo;s process model), &lt;code>nodes&lt;/code> (its hardware abstraction), and volumes (its storage abstraction) &amp;ndash; Fundamentals we can use to build up to &lt;em>workloads&lt;/em> and the controllers used to manage them.&lt;/p>
&lt;p>Until next time.&lt;/p>
&lt;p>&amp;ndash;dave&lt;/p></description></item></channel></rss>